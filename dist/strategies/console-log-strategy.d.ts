import { LogStrategy, LogEntry } from '../types';
export declare class ConsoleLogStrategy implements LogStrategy {
    private colors;
    private reset;
    log(entry: LogEntry): void;
}
//# sourceMappingURL=console-log-strategy.d.ts.map